# Gentoo-kernel-Hardmode-luma
A fully manual Gentoo kernel config built by hand with fine-tuned optimizations across the entire kernel. Minimal, fast, and tailored strictly for AMD CPUs — with all Intel features removed and even unused AMD components disabled to reduce kernel size and keep the build tight and efficient
